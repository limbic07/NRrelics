# 🚀 快速开始指南

## 📋 前置要求

- Python 3.8+
- Windows 10/11 (推荐)
- 4GB+ RAM

## ⚡ 快速安装

### 1. 克隆或下载项目
```bash
cd NRrelics
```

### 2. 创建虚拟环境（可选但推荐）
```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate
```

### 3. 安装依赖
```bash
pip install -r requirements.txt
```

## 🎮 运行应用

### 方式 1：直接运行
```bash
python main.py
```

### 方式 2：使用启动脚本
```bash
python run.py
```

## 📊 启动流程

```
1. 应用启动 (<1秒)
   ↓
2. 显示主窗口
   ↓
3. 后台初始化 OCR 引擎 (5-10秒)
   ↓
4. 控制台显示: ✓ OCR 引擎初始化完成
   ↓
5. 可以使用识别功能
```

## 🎨 界面说明

### 主窗口
- **左侧导航栏**: 页面切换
- **右侧内容区**: 页面内容

### 页面功能

#### 🛍️ 商店筛选
- 点击 "▶ 开始识别" 开始识别
- 点击 "⏹ 停止" 停止识别
- 识别结果显示在下方

#### 📦 仓库清理
- 显示遗物卡片网格
- 卡片包含：名称、品质、数量
- 品质颜色编码：
  - 灰色：普通
  - 蓝色：稀有
  - 紫色：史诗
  - 橙色：传说

#### 💾 存档管理
- 导入存档
- 导出存档
- 删除存档
- 查看存档列表

## 🔧 配置文件

### config/settings.json
```json
{
  "app": {
    "name": "NRrelic Bot",
    "version": "2.0.0",
    "description": "黑夜君临遗物检测工具"
  },
  "ocr": {
    "relic_type": "normal",
    "similarity_threshold": 0.9,
    "max_retry": 3,
    "data_dir": "data"
  },
  "ui": {
    "theme": "light",
    "window_width": 1200,
    "window_height": 750
  },
  "automation": {
    "delay_after_ocr": 0.1,
    "key_press_duration": 0.1
  }
}
```

## 📁 项目结构

```
NRrelics/
├── main.py                 # 主程序入口
├── run.py                  # 快速启动脚本
├── requirements.txt        # 依赖列表
├── config/
│   └── settings.json       # 配置文件
├── core/
│   ├── ocr_engine.py       # OCR 引擎
│   ├── automation.py       # 自动化控制
│   └── save_manager.py     # 存档管理
├── ui/
│   ├── main_window.py      # 主窗口
│   ├── pages/              # 页面
│   │   ├── page_shop.py
│   │   ├── page_repo.py
│   │   └── page_save.py
│   └── components/         # 组件
│       ├── logger_widget.py
│       └── relic_card.py
└── data/                   # 词条库
    ├── normal.txt
    ├── normal_special.txt
    ├── deepnight_pos.txt
    └── deepnight_neg.txt
```

## 🐛 常见问题

### Q: 应用启动很慢？
A: 首次启动时会初始化 OCR 引擎，这是正常的。后续启动会快得多。

### Q: 如何加快启动速度？
A: 应用已经优化为异步初始化 OCR，UI 会立即显示。

### Q: OCR 初始化失败怎么办？
A: 检查控制台错误信息，确保已安装所有依赖。

### Q: 如何修改窗口大小？
A: 编辑 `config/settings.json` 中的 `window_width` 和 `window_height`。

### Q: 如何切换遗物类型？
A: 编辑 `config/settings.json` 中的 `relic_type`（"normal" 或 "deepnight"）。

## 💡 使用技巧

### 1. 快速启动
```bash
python run.py
```

### 2. 查看日志
控制台会显示 OCR 初始化进度和识别结果。

### 3. 自定义配置
编辑 `config/settings.json` 调整参数。

### 4. 添加词条
编辑 `data/` 目录下的词条文件。

## 🔗 相关文件

- [完整更新说明](CHANGELOG_v2.md)
- [项目结构](PROJECT_STRUCTURE.md)
- [更新总结](UPDATE_SUMMARY.md)

## 📞 获取帮助

1. 查看控制台输出
2. 检查配置文件
3. 查看项目文档

## ✨ 主要特性

- ⚡ 极速启动（<1秒显示 UI）
- 🎨 Windows 11 Fluent Design
- 🔄 异步 OCR 初始化
- 📊 遗物卡片网格展示
- 🎯 现代化交互设计

---

**祝你使用愉快！** 🎉
